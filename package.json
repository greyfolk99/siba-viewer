{
  "name": "siba-viewer",
  "displayName": "SIBA Viewer",
  "description": "Live preview for SIBA (Structured Ink for Building Archives) documents",
  "version": "0.1.0",
  "publisher": "hjseo",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:markdown"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "siba-viewer.openPreview",
        "title": "SIBA: Open Preview",
        "icon": "$(open-preview)"
      },
      {
        "command": "siba-viewer.openPreviewToSide",
        "title": "SIBA: Open Preview to the Side",
        "icon": "$(open-preview)"
      },
      {
        "command": "siba-viewer.openGraph",
        "title": "SIBA: Open Graph View",
        "icon": "$(type-hierarchy)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "when": "resourceLangId == markdown",
          "command": "siba-viewer.openPreviewToSide",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "SIBA Viewer",
      "properties": {
        "siba-viewer.lspPath": {
          "type": "string",
          "default": "siba-lsp",
          "description": "Path to the siba-lsp executable"
        },
        "siba-viewer.autoRefresh": {
          "type": "boolean",
          "default": true,
          "description": "Automatically refresh preview on document change"
        },
        "siba-viewer.refreshDelay": {
          "type": "number",
          "default": 300,
          "description": "Delay in milliseconds before refreshing preview after a change"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  }
}
